[project]
name = "aria-pipeline"
version = "0.1.0"
description = "Pipeline agentique de constat d'état des lieux (Modules A, B, C, D)."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain-core>=0.3.68",
    "langchain-openai>=0.3.16",
    "langgraph>=0.4.7",
    "pydantic>=2.11.4",
    "python-dotenv>=1.0.0",
    "reportlab>=4.3.1",
    "pillow>=10.0.0",
]

[tool.uv]
package = false
