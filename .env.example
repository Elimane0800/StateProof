# Required for live inspection (ARIA pipeline, Modules A/B/D call the VLM).
# docker compose interpolates this from the repo-root .env.
NVIDIA_API_KEY=your_key_here

# Optional: override the default NVIDIA vision model.
# NVIDIA_MODEL=meta/llama-3.2-90b-vision-instruct
